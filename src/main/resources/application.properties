spring.application.name=CodeCafe

# Multipart upload limits — must be >= the 25 MB per-file limit enforced by FileValidatorService
spring.servlet.multipart.max-file-size=26MB
spring.servlet.multipart.max-request-size=1300MB
